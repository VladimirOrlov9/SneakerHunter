version: '3.1'

services:
  app:
    container_name: springboot-postgresql
    image: springboot-postgresql
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
